import math
n,r = list(map(int,input().split()))
points = list(map(int,input().split()))
max_y=2*r
circles=[]
circles.append([points[0], 2*r])

for i in range(1,n):
    x=points[i]
    y=math.pow(2*r,2)-math.pow(x-)
    circles.append()
